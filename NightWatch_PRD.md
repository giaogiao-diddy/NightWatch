## 产品需求文档（PRD）

### NightWatch：面向现代实时湖仓架构的夜间值守与自愈智能体

---

### 1. 产品概述

| 项目     | 内容                                                                                                       |
| -------- | ---------------------------------------------------------------------------------------------------------- |
| 产品名称 | NightWatch（守夜人）                                                                                       |
| 产品定位 | 面向现代湖仓与实时数仓架构的数字值夜 Agent，专为解决传统数据团队“凌晨起夜维护数仓”痛点而生，提供夜间无人值守的诊断、自愈、熔断与责任路由能力 |
| 目标用户 | 数据开发工程师、数仓平台工程师、实时计算工程师、数据团队负责人、核心业务看板 owner                           |
| 核心价值 | 在不依赖人工半夜盯盘和手工排障的前提下，让企业获得覆盖日志归因、自动恢复、Schema Drift 熔断与精准通知的一体化 Data AIOps 能力 |

---

### 2. 背景与痛点

| 痛点编号 | 描述                                                                 | 当前解决方式                               | 问题                                                                 |
| -------- | -------------------------------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------- |
| P1       | 凌晨 2 点 Spark 核心任务因数据倾斜触发 Executor OOM，核心经营看板断流 | 值班同学被电话叫醒，手动登录 YARN 拉日志排查 | 日志分散、定位慢、恢复高度依赖个人经验，MTTR 常常以小时计             |
| P2       | Spark Driver 资源争抢或 GC 抖动导致作业长时间卡死                    | 观察监控后手工调整内存和并发参数重新提交     | 诊断强依赖资深工程师，普通值班同学难以区分是代码问题还是资源问题     |
| P3       | Flink CDC 夜间同步过程中，上游 MySQL 静默改表导致任务全局失败        | 第二天业务方发现报表异常后再回溯排查         | 故障发现滞后，脏数据可能已经进入 Paimon 与下游 StarRocks 查询链路     |
| P4       | Paimon 写入压力突增导致 checkpoint 抖动、sink 背压和批流链路雪崩     | 人工查看 Flink Web UI 与指标面板逐项定位     | 现场信息碎片化，排查路径长，无法快速判断是否应该自动扩容或降载       |
| P5       | 夜间告警风暴下，Slack、飞书、邮件同时轰炸多个群                      | 人工在群里同步、拉人、转发日志               | 告警疲劳严重，责任边界模糊，数据团队常在第二天成为默认背锅方         |
| P6       | 上游字段重命名或删除后，下游链路未及时熔断，导致污染数据进入指标层   | 依赖人工发现后停任务、回滚数据               | 爆炸半径扩大，修复成本从单任务级别上升为全链路数据回补               |
| P7       | 常见瞬态故障其实可以自动重试，但缺乏安全受控的闭环机制               | shell 脚本简单重试或完全不重试               | 盲目重试会放大故障，完全不重试又会制造大量无效夜间人工介入           |
| P8       | 故障归因与修复经验沉淀在少数专家脑中，新人很难接手夜间运维            | 口口相传、Wiki 零散记录                      | 团队规模扩大后无法复制，值班质量波动极大                             |

---

### 3. 产品架构

    ┌─────────────────────────────────────────────────────────────────────┐
    │                           用户交互与告警层                           │
    │   飞书 / Slack / Jira / GitHub Issue   │   Web Console / API        │
    └──────────────────────────┬──────────────────────────────────────────┘
                               │
    ┌──────────────────────────▼──────────────────────────────────────────┐
    │                           感知与事件接入层                           │
    │  YARN / Spark History  Flink CDC  Airflow / DolphinScheduler       │
    │  Metrics / Logs / Lineage / Catalog Events / Schema Change Events  │
    └──────────────────────────┬──────────────────────────────────────────┘
                               │
    ┌──────────────────────────▼──────────────────────────────────────────┐
    │                    Agent 推理层（LangGraph 诊断环）                  │
    │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐   │
    │  │ 事件拦截器   │  │ 混合检索 RAG │  │ RCA 决策器               │   │
    │  │ 告警聚合去噪 │  │ BM25 + 向量  │  │ OOM / 倾斜 / 争抢 / 漂移 │   │
    │  └──────────────┘  └──────────────┘  └──────────────────────────┘   │
    │  ┌──────────────────────────────┐  ┌────────────────────────────┐   │
    │  │ 历史修复记忆库               │  │ Pydantic 结构化修复输出    │   │
    │  │ Qdrant + 规则知识库          │  │ 调优参数 / 熔断动作 / 通知 │   │
    │  └──────────────────────────────┘  └────────────────────────────┘   │
    └──────────────────────────┬──────────────────────────────────────────┘
                               │
    ┌──────────────────────────▼──────────────────────────────────────────┐
    │                         安全执行与重试层                             │
    │  Try-Heal-Retry Orchestrator                                        │
    │  ┌─────────────┐  ┌──────────────┐  ┌───────────────────────────┐   │
    │  │ 参数护栏    │  │ 熔断控制器   │  │ 回滚 / 重试 / 隔离策略    │   │
    │  │ 白名单校验  │  │ 任务暂停终止 │  │ 限次执行 + 审计留痕       │   │
    │  └─────────────┘  └──────────────┘  └───────────────────────────┘   │
    └──────────────────────────┬──────────────────────────────────────────┘
                               │
    ┌──────────────────────────▼──────────────────────────────────────────┐
    │                        MCP Server / 执行接入层                       │
    │  FastMCP Tools:                                                     │
    │  Spark Submit / YARN / Flink CDC / DolphinScheduler / Airflow       │
    │  Hive Metastore / Paimon Catalog / StarRocks / GitHub / Jira        │
    └─────────────────────────────────────────────────────────────────────┘

---

### 4. 功能需求（按优先级分期）

#### MVP（第一期）—— 核心闭环

| 功能ID | 功能名称                | 描述                                                                 | 优先级 |
| ------ | ----------------------- | -------------------------------------------------------------------- | ------ |
| F1.1   | 夜间告警接入            | 接入 Spark、Flink、Airflow、DolphinScheduler 的失败事件与告警流      | P0     |
| F1.2   | 日志聚合与上下文采集    | 自动拉取 YARN 日志、Spark History、Flink Job 异常堆栈与关键指标      | P0     |
| F1.3   | 混合检索诊断            | 基于 BM25 + Qdrant 向量检索召回历史故障与操作手册，辅助 RCA          | P0     |
| F1.4   | LangGraph RCA 工作流    | 构建事件拦截 → 检索 → 诊断 → 输出建议的状态机闭环                    | P0     |
| F1.5   | 故障类型识别            | 自动识别 Spark OOM、数据倾斜、Driver 争抢、Flink CDC Schema 异常等   | P0     |
| F1.6   | 结构化诊断输出          | 使用 Pydantic 约束输出诊断结论、建议动作、风险等级与证据链           | P0     |
| F1.7   | 值班摘要面板            | 在 Web Console 展示故障摘要、根因判断、影响任务和推荐动作            | P0     |
| F1.8   | 人工确认执行            | MVP 阶段仅支持人工确认后执行修复动作，形成可控的人机协同闭环         | P0     |

#### 第二期 —— 自动化恢复

| 功能ID | 功能名称                | 描述                                                                 | 优先级 |
| ------ | ----------------------- | -------------------------------------------------------------------- | ------ |
| F2.1   | Try-Heal-Retry 引擎     | 诊断完成后自动生成修复计划并触发受控重试                            | P0     |
| F2.2   | Spark 参数动态调优      | 生成 spark.executor.memory、shuffle partitions、并发相关调优参数     | P0     |
| F2.3   | Flink 作业恢复策略      | 根据 checkpoint、背压、sink 延迟自动选择重启、限流或隔离策略         | P0     |
| F2.4   | MCP 原位操作            | 通过 FastMCP 调用调度器与执行引擎接口，直接修改任务参数并重新拉起    | P0     |
| F2.5   | 修复动作白名单          | 限定 Agent 仅可修改预定义参数与动作，防止越权或危险操作              | P1     |
| F2.6   | 自动重试结果回写        | 将修复结果、恢复耗时、最终状态回写至事件中心和知识库                | P1     |
| F2.7   | 历史修复记忆复用        | 相似故障优先复用历史成功方案，提高二次诊断与修复成功率              | P1     |

#### 第三期 —— 熔断与通知

| 功能ID | 功能名称                | 描述                                                                 | 优先级 |
| ------ | ----------------------- | -------------------------------------------------------------------- | ------ |
| F3.1   | Schema Drift 实时识别   | 捕获上游 DDL、字段漂移、类型不兼容等高风险结构变更                  | P0     |
| F3.2   | 物理熔断                | 对 Flink CDC、下游消费 DAG 或写湖任务执行暂停、终止或隔离            | P0     |
| F3.3   | 数据污染隔离            | 对疑似脏批次进行 Quarantine，阻止落入 Paimon 和 StarRocks            | P0     |
| F3.4   | 血缘爆炸半径分析        | 结合数据目录与表级、列级血缘评估受影响的报表、任务与业务域           | P0     |
| F3.5   | GitHub / Jira 精准提单  | 自动生成问题描述、证据链与责任上下文，直接艾特相关后端或数据 owner   | P1     |
| F3.6   | 业务影响通知            | 向核心看板 owner 发送自然语言通知，说明停更时间、影响范围与处理状态   | P1     |
| F3.7   | 防推诿审计链            | 记录变更来源、决策过程、执行动作与最终恢复结果，支持复盘追责         | P1     |

---

### 5. 非功能需求

| 维度         | 要求                                                                 |
| ------------ | -------------------------------------------------------------------- |
| 诊断时效     | 夜间告警触发后 60 秒内完成首轮 RCA 输出                               |
| 自动恢复时效 | 对可自愈场景，5 分钟内完成修复动作下发与首次重试                     |
| 安全性       | 所有自动化动作必须经过白名单校验、参数边界校验与审计日志记录         |
| 可解释性     | 每次诊断必须输出证据链，包括日志片段、关键指标、匹配历史案例         |
| 可扩展性     | 新增一种执行引擎或调度器适配器的开发量不超过 3 人天                  |
| 高可用性     | 核心 Agent 控制面需支持单点故障切换，夜间值守能力不依赖单实例         |
| 合规性       | 运行日志、库表元数据与通知内容必须支持脱敏与权限控制                 |
| 部署方式     | MVP 支持单机 Docker 部署；生产支持 K8s 与多租户环境                  |
| 观测能力     | 全链路保留事件、推理、修复、重试与熔断审计信息，支持复盘与评估       |

---

### 6. 技术选型

| 组件           | 选型                                               | 理由                                                                 |
| -------------- | -------------------------------------------------- | -------------------------------------------------------------------- |
| Agent 框架     | LangGraph                                          | 适合构建夜间故障处理的有向状态机，天然支持诊断、分支决策与重试闭环   |
| 后端框架       | Python + FastAPI                                   | 生态成熟，便于暴露控制台 API、Webhook 接口与运维查询接口             |
| MCP 协议层     | FastMCP                                            | 统一封装执行引擎、调度器与工单系统能力，便于 Agent 安全调用          |
| 结构化输出     | Pydantic                                           | 对修复动作、熔断指令和通知体进行强约束，降低 LLM 幻觉风险            |
| 向量库         | Qdrant                                             | 用于存储历史故障、修复案例、规则知识和值班手册，检索性能稳定         |
| 检索策略       | BM25 + 向量检索                                    | 同时兼顾日志关键词命中与故障语义相似度，适合复杂 RCA 场景            |
| 批处理计算引擎 | Apache Spark                                       | 企业数仓夜间批任务核心引擎，OOM、倾斜、Driver 争抢是主要诊断对象     |
| 流处理引擎     | Apache Flink                                       | 承载实时链路与 CDC 作业，是 Schema Drift 和写入背压的重要观测对象    |
| CDC 框架       | Flink CDC                                          | 捕获 MySQL 等上游库表变更，是模式漂移预警和熔断的关键入口            |
| 湖仓存储       | Apache Paimon                                      | 高吞吐流式写入与主键表能力强，适合承接实时数据落湖与回溯             |
| 实时分析引擎   | StarRocks                                          | 承接下游查询与看板分析，便于体现故障对业务指标链路的影响             |
| 元数据与血缘   | Hive Metastore + 统一数据目录                      | 提供库表、字段、血缘与 owner 信息，支撑爆炸半径分析和精准通知         |
| 调度系统       | Apache Airflow / DolphinScheduler                  | 负责工作流编排，是自动重试、暂停、终止和重新调度的主要执行面         |
| 可观测性       | Langfuse + Prometheus + Grafana                    | 追踪 Agent 推理链路、模型表现与系统指标，支撑效果评估与运维监控      |
| 通知与协同     | GitHub Issues / Jira / Slack / 飞书                | 对研发、数据 owner 与业务方做精准路由与闭环协同                     |

---

### 7. 数据流（MVP 核心链路）

    场景：凌晨 3 点，核心用户增长看板对应的 Spark 汇总任务失败，YARN 告警触发 NightWatch

    │
    ▼
    [1] 事件接入：NightWatch 接收 Spark 任务失败事件，抓取 application id、失败 stage、executor 日志、GC 指标
    │
    ▼
    [2] 告警聚合：将同一批次的 OOM、shuffle failed、container lost 告警聚合为单一事件故事
    │
    ▼
    [3] 混合检索：检索历史相似案例，命中“数据倾斜导致单分区过大，引发 Executor OOM”的历史记录
    │
    ▼
    [4] LangGraph 诊断：结合日志、分区分布、任务并发和 executor 失败模式，判定根因为 skew + memory 配置偏低
    │
    ▼
    [5] 结构化输出：生成修复 JSON，包括提高 spark.executor.memory、调整 spark.sql.shuffle.partitions、开启 AQE 相关参数
    │
    ▼
    [6] 安全校验：参数进入白名单校验和上下界检查，确认动作属于允许自动修复范围
    │
    ▼
    [7] MCP 执行：通过调度器 MCP Tool 原位更新任务运行参数，并触发一次受控重试
    │
    ▼
    [8] 结果验证：监控重试任务是否恢复、关键下游表是否按 SLA 产出、StarRocks 查询是否恢复正常
    │
    ▼
    [9] 事件回写：记录“夜间自动修复成功”，回写知识库、控制台和通知渠道；若失败则升级为人工介入并附完整证据链

---

### 8. 成功指标

| 指标                   | MVP 目标     | 说明                                                         |
| ---------------------- | ------------ | ------------------------------------------------------------ |
| 夜间人为干预次数下降比率 | ≥ 60%        | 对比引入 NightWatch 前后的夜间电话叫醒与人工登录排障次数     |
| MTTR 故障恢复时间缩短   | ≥ 70%        | 从故障发生到恢复完成的平均时间显著下降                       |
| 首轮 RCA 准确率         | ≥ 80%        | 在典型 Spark / Flink / Schema Drift 场景上的根因判断准确率   |
| 自动修复成功率          | ≥ 50%        | 在可纳入白名单的常见资源与配置类故障中，自动修复闭环成功比例 |
| 高风险漂移拦截率        | 100%         | 所有识别到的破坏性 Schema Drift 必须触发阻断或升级流程       |
| 告警降噪率              | ≥ 70%        | 聚合同源噪声告警，减少群消息与重复工单数量                   |
| 精准责任路由率          | ≥ 85%        | 提单能直接命中正确 owner、研发责任人或业务看板负责人         |
| 平均首轮诊断时长        | ≤ 60 秒      | 从告警进入到输出第一版 RCA 结论的时间                        |
| 知识库复用命中率        | ≥ 40%        | 历史修复方案在相似故障中被成功复用的比例                     |

---

### 9. 里程碑计划

| 阶段           | 交付物                                                                 | 周期     |
| -------------- | ---------------------------------------------------------------------- | -------- |
| M1：感知骨架   | FastAPI 后端、告警接入、日志采集模块、LangGraph 骨架、基础控制台       | 第 1 周  |
| M2：诊断闭环   | 混合检索、RCA 状态机、Pydantic 结构化输出、知识库写入                  | 第 2 周  |
| M3：自动恢复   | Try-Heal-Retry 引擎、Spark 参数调优、MCP 执行器、重试审计              | 第 3-4 周 |
| M4：熔断通知   | Schema Drift 检测、Flink CDC 熔断、血缘影响分析、GitHub 或 Jira 提单   | 第 5 周  |
| M5：生产增强   | 权限控制、审计看板、指标评估体系、K8s 部署与高可用优化                 | 第 6 周  |

---

### 10. 风险与应对

| 风险                                   | 概率 | 影响                 | 应对                                                                 |
| -------------------------------------- | ---- | -------------------- | -------------------------------------------------------------------- |
| LLM 对复杂分布式故障的诊断出现误判     | 中   | 错误修复或误报       | 使用结构化证据链、历史案例检索、规则兜底，并在 MVP 阶段保留人工确认 |
| 自动调优参数越界导致任务进一步恶化     | 中   | 放大故障             | 建立白名单、参数上下界与灰度重试机制，超出阈值直接阻断自动执行      |
| Flink CDC 复杂 DDL 无法稳定识别        | 中   | 漂移漏报或误熔断     | 对高风险 DDL 采用严格策略，结合 Catalog 变更事件和运行时异常双通道检测 |
| 血缘信息缺失导致责任路由不准确         | 中   | 通知错误对象         | 引入统一目录、owner 元数据补录与人工纠偏反馈机制                    |
| MCP 执行层权限过大带来安全风险         | 高   | 误操作生产系统       | 工具最小权限化、审计日志、分环境隔离、审批开关与紧急熔断机制         |
| 日志体量过大导致首轮诊断超时           | 中   | 夜间处理时效下降     | 采用日志切片、优先级摘要、混合检索压缩与异步上下文补全机制           |
| 团队过度依赖自动化，忽略人工复盘积累   | 低   | 体系长期退化         | 建立值班复盘制度，持续更新知识库与规则模板                           |
| 多调度器、多引擎接入复杂度超预期       | 中   | 研发延期             | 先聚焦 Spark + Flink + 单一调度器打通闭环，再逐步扩展适配器         |

---

这份 PRD 覆盖了 NightWatch 从夜间告警接入、RCA 诊断、Try-Heal-Retry 自动恢复，到 Schema Drift 物理熔断与精准通知的完整闭环。要我下一步继续基于这份 PRD，补一版 TECH_DESIGN.md 或者 AGENTS.md 吗？